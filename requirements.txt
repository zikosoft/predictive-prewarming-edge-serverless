# Pinned to the exact versions used to produce the results in results/
# (verified via `pip show` in the benchmarking environment). Re-run with
# these versions for an exact reproduction; the harness has no known
# sensitivity to minor-version drift in these libraries, but pin exactly
# if you want a byte-identical CSV comparison.
fastapi==0.141.1
uvicorn==0.46.0
aiohttp==3.14.3
psutil==7.2.2
pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.9
